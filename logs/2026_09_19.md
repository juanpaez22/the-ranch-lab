# 2026-09-19: Lab access, WSL follow-up, and first application

- Confirmed the repository was clean on `main` and the k3s API on `bronco` was healthy. `yeti` was temporarily unreachable because it was disconnected; the owner later reported access restored.
- Configured the workstation WSL SSH client to use its existing key for the laptop host aliases and verified remote command access to `bronco`. No credentials or LAN addresses were added to the repository.
- Reviewed direct LAN access to `longhorn` WSL. The current Ubuntu instance has systemd enabled but no SSH server installed; no WSL networking, firewall, startup, or service changes were applied.
- Added a backlog item and primary references for mirrored WSL networking, service lifecycle, a possible k3s GPU worker, and exposing services such as Ollama to lab peers.
- Selected IT-Tools as the first disposable LAN application. The initial test will use the bundled Traefik and ServiceLB through `bronco.local` on ordinary HTTP; no additional load balancer or LAN DNS component is required for this validation.
- Reviewed the Netgear RAX45 DNS behavior and confirmed that its custom DNS setting selects an upstream resolver rather than creating local records. Cluster-hosted LAN DNS remains technically possible but is deferred to avoid coupling household DNS availability to the initial single-server k3s cluster.
- Added a Kustomize-managed IT-Tools application with one replica, health probes, resource bounds, a ClusterIP Service, and a hostless Traefik Ingress. Applied it successfully and pinned the resolved container image digest after the first pull.
- Verified the deployment Ready on bronco with no restarts, a populated EndpointSlice, and HTTP 200 through ServiceLB and Traefik. WSL could not resolve `bronco.local`, so validation preserved that Host header while supplying bronco's reserved address explicitly. Yeti was NotReady, so cross-node and separate LAN-client validation remain pending.
- Yeti returned Ready during the digest-pinned rollout, and Kubernetes placed the replacement IT-Tools pod there while Traefik remained on bronco. Repeated ingress validation returned HTTP 200 across nodes; an in-pod request through `it-tools.it-tools.svc.cluster.local` also returned HTTP 200, confirming cluster DNS and Service routing. The earlier probe warnings were transient startup checks; the final pod was Ready with no restarts.
- Chose standalone Pi-hole on the Raspberry Pi as the next homelab platform project so applications can use friendly `apps.home.arpa` names instead of node names and ports.
