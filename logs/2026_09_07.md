# 2026-09-07: Lab bootstrap and k3s installation

## Repository and laptop preparation

- Created the public `the-ranch-lab` repository with a brief README, agent instructions, operational scripts/configs, documentation, and logs. Added the bull mascot. Kept credentials and real LAN addresses outside Git.
- Initial SSH checks failed because access used passwords and one hostname lacked an accepted host key. The owner configured SSH keys and host trust; subsequent noninteractive checks succeeded on both laptops.
- Inventoried CPU, memory, NVMe disks, model, OS, and services via read-only SSH. The current specifications are in [architecture](../docs/ARCHITECTURE.md).
- Staged the versioned lid configuration; the owner installed it and rebooted using interactive sudo. Both laptops returned with kernel `7.0.0-31-generic`, no failed system units, and no pending reboot markers. The earlier bronco GRUB boot-record failure disappeared after reboot; its cause was not diagnosed.
- Verified installed lid settings; owner confirmed SSH with lids closed. Curl was installed by the owner. Complete package-update coverage was not independently audited.

## Cluster bootstrap

- Reviewed official k3s requirements and configuration choices. Approved one server on bronco and one agent on yeti, defaults otherwise, plus `bronco.local` TLS SAN and secrets encryption.
- Retained host swap after correcting the overly broad initial suggestion to disable it. Accepted no backups for disposable experiments; Git cannot restore lost application data or secrets.
- Selected official stable `v1.36.4+k3s1`. Pinned both installer tag and binary release. Replaced an overcomplicated role wrapper with two minimal scripts at the owner's request.
- Staged and syntax-checked scripts; owner ran privileged installation. The agent token was supplied privately, not stored in Git or chat.
- Agent startup initially stalled using `bronco.local`: shell mDNS and direct curl worked, while system DNS and the agent proxy failed. The owner switched the endpoint to the reserved server IP and restarted the agent; registration succeeded without reinstalling.
- Updated the agent template/installer to accept the address privately while keeping the public template generic. Future installs use this corrected path.

## Connection interruption

- Around 21:24-21:26 Pacific, yeti lost cluster connectivity and recovered automatically at 21:26:19-20. Owner also reported brief SSH interruptions.
- Neither laptop rebooted or restarted SSH. No failed units, observed OOM/suspend events, or resource pressure explained the incident.
- Yeti logged a NetworkManager assertion at 21:24:41 without a service restart. That is correlation, not a confirmed cause. No physical Wi-Fi disconnect appeared in the inspected laptop logs; Windows event queries returned no matching records.
- Short peer tests had zero loss but latency varied roughly 3-309 ms. Subsequent checked logs had no further matching cluster errors. Sustained network reliability remains unproven.

## Administration and verification

- Owner output confirmed both nodes Ready, expected system pods Running/Completed, and secrets encryption enabled with matching hashes.
- Corrected advice that the k3s-provided kubectl wrapper would automatically use the user's copied config: this installation still selected root-owned defaults. Owner installed checksum-verified standalone kubectl v1.36.4 in `~/.local/bin` on both laptops, using private kubeconfigs; ordinary kubectl worked after login refreshed PATH.
- Installed checksum-verified native Windows kubectl and copied the working config securely. TLS failed because local HTTPS inspection substituted the server certificate; certificate comparison showed WSL received the original certificate.
- Installed the matching standalone client and private config in WSL. Verified both nodes Ready, all expected system pods healthy, and authenticated API readiness `ok` from PowerShell through WSL. No certificate-verification bypass or security-product change was applied.
- Native Windows client remains downloaded but not on PATH. WSL is an administration client, not a cluster node.

## Documentation cleanup and handoff

- Consolidated ten topic logs into this daily file and six docs into architecture, setup, and backlog. Added guidance to append to one log per local day and prefer existing documents.
- Preserved operational procedures, source references, current decisions, and unresolved work. Updated navigation and the lid config's documentation comment; configuration values and scripts were unchanged.
- Verified all local Markdown links/anchors and whitespace checks; confirmed scripts and k3s configuration have no changes.
- Saved the requested WSL community discussion in [backlog](../docs/BACKLOG.md). Prior detailed files remain recoverable in Git history.
- Next work: disposable application tests for cross-node networking, DNS, ingress, node downtime, and recovery; live firewall review. Revisit network instability if it recurs.
